# Append-only audit trail of every attempted agent tool call, successful or rejected.
# Exists so the guardrails are evidenced rather than asserted: a rejection with no record is
# indistinguishable from a call that never happened.
