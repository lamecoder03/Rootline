# Package root for the agent side of the project: guardrails, tool definitions, the investigation loop and rendering.
# Exists so the walls are importable as `agent.guardrails.*` before anything can test them.
